import alpha from './alpha'
import states from './stateList'
import countries from './countryList'

const puzzles: {name: string, words: string[]}[] = [{name: "states", words: states}, {name: "countries", words: countries}]
export {alpha, puzzles}